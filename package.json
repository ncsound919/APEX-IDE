{
  "name": "apex-ide",
  "version": "1.0.0",
  "description": "APEX IDE - A hip-hop themed desktop IDE with Megacode integrations",
  "main": "index.html",
  "scripts": {
    "start": "npx serve . -p 3000",
    "dev": "npx serve . -p 3000 --no-clipboard",
    "backend": "cd backend && npm start",
    "backend:install": "cd backend && npm install",
    "full": "npm install --prefix backend && (node backend/server.js &) && npx serve . -p 3000"
  },
  "keywords": [
    "ide",
    "apex",
    "megacode",
    "llm",
    "hip-hop"
  ],
  "author": "ncsound919",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
